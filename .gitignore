# Built binaries live in GitHub Releases, never in git (keeps the repo tiny and
# the source easy to audit). `./build.sh` drops them here.
/dist/

# Cross-compiled binaries if built in-tree
kibitz-relay
kibitz-relay-*
*.exe
*.aar

# A running relay's persisted identity + emitted link
relay-state.json
relay-link.txt
